== ДЗ 1
2026.01.16

. (+) Подготовил требования: ФТ, НФТ, риски
. (+) Спроектировал высокоуровневую архитектуру: подготовил контекстную диаграмму.
. (+) Выбран архитектурный стиль, подготовлено описание и обоснование.
. (+) Подготовлено описание пользователей решения
. (+) Описаны внешние интеграции
. (+) Подготовлено описание потоков данных, высокоуровневые взаимодействия и критичность компонентов.
. (+) Подготовлено описание модели данных

== ДЗ 2
2026.02.09

. (+) Выполнены расчёты показателей производительности (doc 2.07).
. (+) Детализирована концептуальная схема архитектуры (doc 2.08) с учётом высокой доступности, отказоустойчивости, масштабирования и производительности.
. (+) Расширена система мониторинга в LikeC4.
. (+) Описана observability (doc 2.09): требования к системным и бизнесовым метрикам, алерты и пороги.
. (+) Разработан план реагирования на отклонения (doc 2.10): за чем наблюдаем , пороги и действия, эскалация L1–L3, runbook.
. (+) Разработан план DR (doc 2.10): RTO/RPO по классам данных, резервное копирование , сценарии отказов, роли и контакты; мероприятия по проверке DR .
. (+) В LikeC4 детализированы пользователи.
. (+) Добавлены протоколы и описания взаимодействия с внешними системами в LikeC4.
. (+) Добавлены виды диаграмм LikeC4 и экспорт в PlantUML. 
. (+) Схемы доступны для интерактивного просмотра: https://shelepovg.github.io/system-design-homework/

== ДЗ 3
2026.02.23

. (+) Выполнен перерасчёт пиковой и средней нагрузки согласно замечанию по ДЗ 2, в файле _2.07 Расчеты показателей решения_
. (+) Выполнен расчёт объёмов данных по замечанию ДЗ 2 в файле _2.07 Расчеты показателей решения_
. (+) В LikeC4 добавлена связь user-service → IAM; связи API Gateway приведены к маршрутам на микросервисы (user-service, request-processing-service, payment-service, notification-service); для всех связей заданы протоколы из specs (https, http, tcp) и теги #sync / #async
. (+) В документе _2.11 Схема взаимодействия_ добавлена древовидная схема потока запроса с указанием sync/async по компонентам; добавлена подсекция про схемы взаимодействия в формате OpenAPI (Swagger)
. (+) В документе _2.12 Форматы взаимодействия_ добавлена ссылка на спецификацию OpenAPI и согласование с моделью данных
. (+) Добавлена спецификация OpenAPI 3.0 (_openapi/api-gateway.yaml_): ключевые endpoint'ы /api/v1/users, /api/v1/requests, /api/v1/payments, /api/v1/notifications; схемы данных согласованы с моделью данных (doc 2.06)
. (+) В _2.07_ добавлен учёт формата взаимодействия (п. 1.1.1): соотношение «1 бизнес-событие → несколько технических запросов», таблица по сценариям, оценка внутренних технических запросов
. (+) В _2.08_ добавлен раздел «Размещение Rate Limiter, Retry, Circuit Breaker»: таблицы по месту применения, параметрам и сводка по компонентам
. (+) В _2.09 Observability_ дополнены требования к метрикам: метрики по API и RPS (связь с 2.07, OpenAPI); метрики устойчивости — Rate Limiter (429, топ user/IP), Retry (повторы, успех после N попыток), Circuit Breaker (состояние, отказы); ссылки на 2.08 и 2.10

== ДЗ 4
2026.02.28

. (+) Добавлена модель данных в формате DBML (_dbml/model.dbml_): схема таблиц по доменам (users, requests, payments, notifications, audit), согласованная с документом 2.06; возможность просмотра в DBML CLI / dbdiagram.io и экспорта в SQL.
. (+) Создан документ _2.14 Схемы данных_: указание расположения схем (2.06 — концептуальная и логическая модель; dbml/model.dbml — визуализация); 2.14 служит точкой входа к артефактам схем.
. (+) Создан и детализирован документ _2.13 Система хранения данных_: выбор технологий (только PostgreSQL + Redis), обоснование; показатели по хранению (объёмы по хранилищам, рост, освобождение, hot/warm/cold, статичные и модифицируемые данные, нагрузка на чтение и запись — связь с 2.07).
. (+) В _2.13_ добавлены разделы: масштабирование и ВД (read replicas, запись в master, Redis Cluster); соответствие CAP (PostgreSQL — CP, Redis — AP); ограничения решения; хранение и наполнение бэкапов, резервные системы (standby); обоснование применимости, применение кэшей (Cache-Aside, таблица ключей/TTL/инвалидация), допустимость шардирования, обеспечение консистентности; геораспределённость, standby, бэкапы и распределённая обработка (п. 0.6).
. (+) В LikeC4 обновлена система хранения: разделение PostgreSQL на _PostgreSQL (master)_ и _PostgreSQL (read replicas)_; добавлено _Хранилище бэкапов_ (отдельное объектное хранилище). Связи микросервисов с БД явно описаны: запись — в master, чтение — с реплик; добавлена репликация бэкапов (postgres-master → backup-storage, опционально cache → backup-storage). Обновлено описание view «Хранение данных».
. (+) В _2.09 Observability_ уточнены требования к метрикам и алертам с учётом 2.13: метрики БД отдельно по master и по read replicas, replication lag (критично для RPO=0); целевой cache hit rate > 80%, опционально разбивка по типам ключей кэша; метрики и алерты по успешности и задержке бэкапов; ссылки на 2.13 и 2.10.

== ДЗ 5
2026.03.15

. (+) Добавлены тесты (likec4, контейнеры, хранилище, связи).
. (+) Заполнен раздел по аутентификации и авторизации в документе _3.01 Схема защиты и управления доступом_: выбор механизма (OAuth 2.0, OIDC, JWT), выдача и проверка токенов, экспирация и обновление.
. (+) Заполнен документ _3.03 Безопасность передачи данных_: TLS, маскирование PII в логах, перечень чувствительных данных.
. (+) Добавлена схема данных в документ (2.14, изображения).
. (+) В _3.01_ описан выбор между RBAC и ABAC, обоснование RBAC с проверкой владельца ресурса.
. (+) Описан процесс добавления и изменения ролей (3.01): кто назначает роли, хранение в IAM и User Service, аудит назначений.
. (+) Зафиксировано использование HashiCorp Vault для хранения секретов (документ _3.04 Хранение и управление секретами_).
. (+) Заполнен раздел observability для безопасности: документ _3.02 Аудит безопасности_ — логирование событий (аутентификация, 401/403, доступ к конфиденциальным данным, смена ролей), метрики аномального поведения, интеграция с 2.09 и 2.10; обновлены ссылки в 2.09 и 2.10.
. (+) Обновлена схема LikeC4 для управления доступами с RBAC: добавлены Vault, связи API Gateway и User Service с Event Bus для событий безопасности, представление _access_control_rbac_.
. (+) Перегенерированы схемы (PlantUML) и добавлены изображения в документы: 2.01, 2.02, 2.08, 2.09, 2.11, 2.13, 2.14, 3.01 (context, backend_container, monitoring_container, db_container, access_control_rbac, data_model).
. (+) Добавлена схема процессов аутентификации и авторизации в _3.01_ (формат по аналогии с 2.11): логин, запрос с JWT, refresh токена, User Service и кэш.
. (+) Добавлена схема хранения и ротации секретов в _3.04_ (формат по аналогии с 2.11): получение секретов при старте, ротация статических и динамических, аудит Vault.
. (+) Создана конфигурация Keycloak: _keycloak/realm-system-design.json_ (realm, токены 15 мин / refresh rotation, сессия 30 мин, brute-force protection, клиенты SPA/mobile/backend, роли ADMIN, OPERATOR, USER, VIEWER, SERVICE, SUPPORT, AUDITOR, BILLING), _keycloak/README.md_, _keycloak/apply-realm.sh_; ссылка в 3.01.
. (+) Создана конфигурация Vault: политики ACL _vault/policies/*.hcl_ по сервисам (api-gateway, user-service, request-processing-service, payment-service, notification-service, audit-service, operator), пример конфигурации Database engine, _vault/README.md_, скрипт _vault/scripts/setup-policies.sh_; ссылка в 3.04.


== ДЗ 6
2026.03.29

. (+) Реализованы и детализированы схемы развёртывания в LikeC4 (`likec4/deployment.c4`): зоны production (edge, регион A с ВМ и Kubernetes, DR, гео-бэкапы), staging, связи между контурами; в узлах зафиксированы метаданные сайзинга (`sizing_*`) и пояснения в описаниях.
. (+) Дополнен документ _4.01 Схема развертывания_: ссылка на `deployment.c4`, принцип разделения ВМ и Kubernetes, таблица сайзинга по узлам deployment, раздел про размещение stateful-сервисов и приложений.
. (+) Добавлена инфраструктура как код Terraform в каталоге `infra/terraform/`: VPC, подсети, шлюз, security groups, ALB, EC2 (app, postgresql, redis), S3, IAM; пример переменных и README.
. (+) Добавлена автоматизация Ansible в `infra/ansible/`: роли установки Docker (и зависимостей) на ВМ приложений, установка и настройка PostgreSQL 15 (initdb, `listen_addresses`, `pg_hba`, пользователь, БД, схема через `community.postgresql`), плейбуки, пример инвентаря, group_vars, README.
. (+) Подготовлены Dockerfile для контейнеризации backend по модели решения: `infra/docker/services/api-gateway/Dockerfile`, универсальный `infra/docker/services/spring-boot/Dockerfile` (JAR передаётся аргументом сборки); образ SPA LikeC4 в контур деплоя не включался.
. (+) Подготовлены манифесты Kubernetes в `infra/k8s/`: namespaces (`api-gateway`, `backend`, `observability`), ConfigMap/Secret с плейсхолдерами интеграций, Deployment и ClusterIP Service для API Gateway и микросервисов, Ingress для `/api`, Kustomize (`kustomization.yaml`).
. (+) Настроен пайплайн GitLab CI (`infra/gitlab/.gitlab-ci.yml`): обязательная проверка `kubectl kustomize infra/k8s`; условная сборка и push образа `api-gateway` при переменной `API_GATEWAY_JAR` (файл JAR извне репозитория); интеграционный тест образа (`/actuator/health`); ручной деплой на `main` (`kubectl apply -k`, при успешном тесте — `set image` и rollout); уведомления в Telegram на стадии `.post` при сбое пайплайна и при успешном деплое (MVP, переменные `TELEGRAM_*`).
. (+) Заполнены документы _4.02 CICD процесс_ и _4.03 Планы тестирования_: соответствие пайплайна и артефактам репозитория, переменные CI/CD, связь с Terraform/Ansible и секретами (3.04), планы тестов и связь с GitHub Actions (Vitest LikeC4).

== Проект
2026.04.05

. (+) Устранено замечание по связке `namespaces.yaml` и Kustomize: в `infra/k8s/kustomization.yaml` подключены ресурсы observability в соответствии с _2.09 Observability_ (Prometheus, Grafana, алертинг через Alertmanager).
. (+) Добавлен каталог `infra/k8s/observability/`: RBAC для Prometheus (`ServiceAccount`, `ClusterRole`, `ClusterRoleBinding`), конфигурация и `Deployment` + `Service` для Prometheus (образ `prom/prometheus`, retention TSDB 30 суток, скрейп self и подов с аннотациями `prometheus.io/*`, интеграция с Alertmanager по DNS кластера).
. (+) Добавлены `ConfigMap` и `Deployment` + `Service` для Alertmanager (`prom/alertmanager`), минимальная маршрутизация алертов (заготовка под приёмники в проде).
. (+) Добавлены `Secret` с учётными данными администратора Grafana (плейсхолдер пароля), `ConfigMap` с provisioning datasource Prometheus, `Deployment` + `Service` для Grafana (`grafana/grafana`), единая точка дашбордов по метрикам из Prometheus.
. (+) В шапке `kustomization.yaml` зафиксировано соответствие документации: полный контур логов (Kafka, Vector, Elasticsearch, Loki) и трассировки (Jaeger/Zipkin, OpenTelemetry Collector) по модели остаётся за пределами данного kustomize; проверка сборки: `kubectl kustomize infra/k8s`.
. (+) Зафиксированы ограничения MVP: данные Prometheus/Alertmanager/Grafana на `emptyDir`, пароль Grafana — сменить в проде; для сбора метрик с микросервисов потребуются аннотации на подах и endpoint метрик (например, Spring Boot Actuator).
. (+) Вопрос «TLS терминируется здесь?» — по документации проекта: нет на этом ресурсе как на внешней границе для клиентов. В doc/3.03 и doc/2.08 TLS termination зафиксирована у load balancer (edge); клиент → LB: HTTPS. Внутри доверенного периметра (LB → Front / API Gateway) TLS опционален (doc/2.11). В файле _ingress-api.yaml_ задана только HTTP-маршрутизация в кластере. Отдельный spec.tls и Secret — если политика кластера требует HTTPS до Ingress-контроллера; это не отменяет смысл «клиентский HTTPS снимается на LB».
. (+) Подготовлена презентация проекта: добавлены `presentations/MyProjectPresentation.adoc` и экспорт в PDF; разделы по целям, контексту и контейнерам, модели данных (связка с обновлённым `doc/images/data_model.png`), хранению, безопасности, observability.
. (+) Подготовлена презентация возможностей likec4: добавлены `presentations/Archops.adoc` и PDF — краткая демонстрация LikeC4 и AsciiDoc для проектирования систем: вводная по Architecture as Code, фрагменты кода модели (контейнер, `database`, связи, `view` и `deployment view`) из `likec4/`, ссылка на опубликованный сайт диаграмм.

