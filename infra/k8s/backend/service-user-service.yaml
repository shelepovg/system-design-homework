apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: backend
  labels:
    app.kubernetes.io/name: user-service
    app.kubernetes.io/part-of: system-design
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: user-service
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
