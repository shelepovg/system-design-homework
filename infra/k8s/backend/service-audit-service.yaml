apiVersion: v1
kind: Service
metadata:
  name: audit-service
  namespace: backend
  labels:
    app.kubernetes.io/name: audit-service
    app.kubernetes.io/part-of: system-design
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: audit-service
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
