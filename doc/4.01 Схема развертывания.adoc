=== Схема развертывания

Диаграмма развёртывания в LikeC4: файл `likec4/deployment.c4`.

==== Схемы развёртывания 

Ниже — ссылки на интерактивные представления `deployment_all`, `deployment_production`, `deployment_staging` из `likec4/views.c4` на опубликованном LikeC4 (GitHub Pages). Растровые экспорты тех же видов — в каталоге `doc/images/` (для вставки в документы без веб-просмотра).

[[fig-deployment-all]]
https://shelepovg.github.io/system-design-homework/view/deployment_all/[Сводное развёртывание `deployment_all` (LikeC4 на GitHub Pages)^] — сводный вид сред *production*, *staging* и географических контуров (edge, регион A, DR, гео-бэкапы).

[[fig-deployment-production]]
https://shelepovg.github.io/system-design-homework/view/deployment_production/[Развёртывание `deployment_production` (LikeC4 на GitHub Pages)^] — окружение *production*: зоны периметра, регион A (ВМ инфраструктуры и кластер Kubernetes), регион B (DR), гео-бэкапы.

[[fig-deployment-staging]]
https://shelepovg.github.io/system-design-homework/view/deployment_staging/[Развёртывание `deployment_staging` (LikeC4 на GitHub Pages)^] — окружение *staging*: упрощённый контур ВМ и Kubernetes для pre-production.

*Production* — несколько вложенных *зон* внутри `environment production`:

* *edge_security* — периметр *WAF / CDN / DDoS* (managed edge; NFR-005); до узла `vm_perimeter` (Nginx LB) в логической модели.
* *region_primary* (регион A) — основной контур: *vm_infrastructure* (периметр LB, Keycloak, PostgreSQL master+replicas, Redis, Kafka, Vault, MinIO для операционных бэкапов, хранилища логов/метрик/трейсов) и *k8s_workloads* (кластер *k8s_prod* с `kubernetes_namespace`: *ns_ingress*, *ns_gateway*, *ns_backend*, *ns_observability*). Multi-AZ внутри региона отражён в подписях узлов.
* *region_dr* (регион B) — второй географический контур: PostgreSQL replicas/standby, Kafka, Redis, сокращённый Kubernetes (*k8s_dr*, *ns_dr_standby*) для warm standby (NFR-007).
* *region_backup_geography* (регион C + копии) — два узла *backup-storage* (*vm_backup_site_b*, *vm_backup_site_c*) для геораспределённых копий снимков/WAL (NFR-006); плюс приём бэкапов в регионе A (*vm_object_storage*).

*Связи между контурами* (в конце `environment production`): deployment-relationships LikeC4 между *именованными* экземплярами `instanceOf` одного логического элемента — регион A → B: репликация PostgreSQL (read tier A → standby B), Kafka (MM2 / cluster linking), Redis (cross-region replica), объектные бэкапы A → гео-площадки B и C. Направление на диаграмме согласовано с типичными потоками DR (источник данных в A, потребитель/копия в B/C). Переключение трафика (DNS/GSLB, масштабирование *k8s_dr*) в рёбрах не дублируется — см. runbooks и 2.08.

*Staging:* зоны *staging_vm* и *staging_k8s* — упрощённый контур без полного DR-трёхрегионного разбиения.

Типы узлов: `likec4/specs.c4` — `deploymentNode environment`, `zone`, `vm`, `kubernetes`, `kubernetes_namespace`. Представления: `likec4/views.c4` — `deployment_production`, `deployment_staging`, `deployment_all`.

*Принцип разделения:* компоненты с сильным состоянием, периметр и IAM — на *виртуальных машинах* вне Kubernetes; горизонтально масштабируемые приложения, Ingress/статика SPA, Fluent Bit, Vector, Grafana, Prometheus scrape, OTel Collector, Alertmanager и агенты APM — в *Kubernetes*. Интеграция с SaaS APM (New Relic/Datadog) — агенты в подах, ingest во внешний SaaS (2.09).

Подробные требования к HA, АЗ и компонентам — в документе *2.08 Схема архитектуры решения* и *2.13 Система хранения данных*.

[[sizing-deployment]]
==== Сайзинг вычислительных ресурсов (CPU, RAM, диск)

Оценки приведены для планирования ёмкости (заказ ВМ, пулов Kubernetes, объектного хранилища). *Итоговые цифры сайзинга* — в блоке `metadata { sizing_* }` узлов в `likec4/deployment.c4` (удобно для инструментов и сводок); *формулы и пояснения* — в `description` тех же узлов.

*Исходные допущения:*

* *NFR-001 / NFR-002 (1.02):* пиковая нагрузка **200k RPS**, средняя **50k RPS**; деградированный режим не ниже **20k RPS**.
* *Доля запросов с обращением к БД:* **~25%** от RPS после кэша на шлюзе и в Redis (ориентир для OLTP).
* *Объёмы данных (2.13):* совокупный горячий слой PostgreSQL порядка **~3–4 TiB** (users, requests, payments, notifications, audit); Redis **150–200 GiB**; узлы `vm_object_storage` / гео-бэкапы в таблице — под **бэкапы БД и WAL** (NFR-006). *Пользовательское* объектное хранилище до **ПБ** (2.13, FR-011) в эту оценку **не входит** — отдельный контур и сайзинг при внедрении полного объёма файлов.
* *Запас по утилизации:* **~30%** к расчётным ядрам/RAM (целевое использование узлов не постоянно выше **80%**, NFR-008).
* *Уточнение:* цифры — *ориентир до нагрузочного тестирования* (4.03); фактический сайзинг подтверждается метриками p95/p99, saturation и бюджетом ошибок.

===== Таблица сайзинга по узлам deployment (Production / Staging)

[options="header",cols="2,1,1,1,1,3"]
|===
| Узел `deployment.c4` | Регион / среда | vCPU (сумм.) | RAM (GiB) | Диск (TiB) | Кратко: база расчёта

| `vm_edge_waf_cdn`
| Edge (managed)
| —
| —
| —
| Пропускная способность по контракту WAF/CDN; эквивалент **≥250k req/s** (пик 200k RPS × 1,25). vCPU заказчика не выделяются.

| `vm_perimeter`
| A
| 32
| 64
| ~0,8
| Кластер **4×** Nginx/L7; **~6–8k RPS/узел** на пике 200k RPS; TLS и буферы.

| `vm_iam`
| A
| 12
| 48
| ~0,15
| Keycloak **3× HA**; **4 vCPU / 16 GiB** на инстанс; auth-introspect **~2–3k RPS** пик.

| `vm_postgresql` (primary + 2 replica)
| A
| 80
| 640
| ~14
| OLTP **~50k** запросов/с к БД; объёмы 2.13; **32 vCPU / 256 GiB** primary, реплики **2×24 vCPU / 192 GiB**.

| `vm_redis`
| A
| 24
| 192
| ~0,5
| Кластер **3 узла**; **150–200 GiB** горячих данных в RAM; **8 vCPU/узел**.

| `vm_kafka`
| A
| 24
| 96
| ~6
| **3 брокера**; события **~30–50k msg/s** + лог-шина; retention **24–72 ч**.

| `vm_vault`
| A
| 12
| 48
| ~0,3
| **3 узла** Raft; **4 vCPU / 16 GiB** на узел.

| `vm_object_storage`
| A
| 32
| 128
| ~40
| MinIO/S3 **4 узла** (erasure); полные снимки + WAL + рост; см. формулу в `deployment.c4`.

| `vm_observability_storage`
| A
| 48
| 192
| ~12
| ES + TSDB + Jaeger; логи/метрики/трейсы при 200k RPS (сэмплинг трейсов).

| `k8s_prod` (control plane)
| A
| 12
| 48
| ~0,5
| **3 узла** etcd/API.

| `k8s_prod` (workers)
| A
| 576
| 2304
| ~9
| **~18 workers**; Gateway + backend + DS агенты; расчёт от RPS и HPA (см. `deployment.c4`).

| `ns_ingress` / `ns_gateway` / `ns_backend` / `ns_observability`
| A (доли `k8s_prod`)
| (включено в workers)
| (включено в workers)
| —
| Доли **~6% / ~8% / ~55% / ~25%** CPU пула workers — в описаниях namespace в `deployment.c4`.

| `vm_dr_postgresql`
| B (DR)
| 48
| 384
| ~9
| Warm standby / реплики; **~60%** read-tier A по CPU/RAM/диску.

| `vm_dr_kafka`
| B
| 16
| 64
| ~3
| Зеркало топиков; **~50%** ресурсов Kafka A.

| `vm_dr_redis`
| B
| 12
| 96
| ~0,2
| Cross-region replica; **~50%** RAM кластера A.

| `k8s_dr` (cp + workers)
| B
| 108
| 432
| ~3,5
| **~15–20%** от worker pool A; warm standby подов.

| `vm_backup_site_b` / `vm_backup_site_c`
| C / гео
| 16 + 16
| 64 + 64
| ~50 + ~50
| Независимые копии объёма `backup_primary_a` + запас **20%**.

| `vm_staging_data`
| Staging
| 16
| 64
| ~2
| **~1/5** от суммы PG+Redis+Kafka prod (один узел).

| `vm_staging_vault` / `vm_staging_lb` / `vm_staging_iam`
| Staging
| 8
| 20
| ~0,3
| Минимальные инстансы для препрода.

| `k8s_staging`
| Staging
| 40
| 152
| ~1,2
| Укороченный control plane + **4 workers**; **~1/18** pool prod.

|===

===== Сводка по контурам (ориентир)

[options="header",cols="2,1,1,1,1"]
|===
| Контур | vCPU (сумм.) | RAM (GiB) | Диск (TiB) | Примечание

| Регион A: VM (без K8s)
| 264
| 1408
| ~74
| Сумма строк `vm_*` в A.

| Регион A: `k8s_prod`
| 588
| 2352
| ~10
| Control plane + workers.

| Регион B (DR), суммарно
| 184
| 976
| ~16
| VM + `k8s_dr`.

| Гео-бэкапы B + C (только хранение)
| 32
| 128
| ~100
| Доминирует диск; CPU/RAM — сервисные узлы объектного хранилища.

| Staging, суммарно
| 64
| 236
| ~3,5
| Без DR-трёхрегионного контура.

|===

*Итого (Production A+B + гео-бэкапы, без edge):* порядка **1068 vCPU**, **~4864 GiB RAM**, **~200 TiB** диска (включая гео-копии бэкапов). Edge — по тарифу managed-сервиса. Staging — отдельно, см. таблицу выше.

==== Анализ соответствия NFR (1.02) и deployment-модели LikeC4

Ниже — сверка критичных NFR (MVP, документ *1.02*) с тем, что явно отражено в `likec4/deployment.c4` (узлы `vm`, `kubernetes`, `kubernetes_namespace`). *Условные обозначения:* ✅ — прямо следует из диаграммы/подписей; ⚠ — частично (нужны другие артефакты или уточнение); ❌ — на deployment-диаграмме не показано (не значит, что не реализуется в проекте).

[options="header",cols="1,3,3,1"]
|===
| NFR | Суть требования (кратко) | Отражение в `deployment.c4` | Оценка

| NFR-001
| Масштаб аудитории, рост нагрузки
| Численные RPS/DAU — в 1.02; *сайзинг* узлов под пик **200k RPS** — в `deployment.c4` (`description`) и <<sizing-deployment,сайзинг 4.01>>.
| ✅

| NFR-002
| Производительность, p95 latency, пропускная способность
| Упоминание бюджета p95 у Gateway в *ns_gateway*; конкретные SLO и нагрузочное тестирование — в 1.02/2.08/4.03.
| ⚠

| NFR-003
| Доступность, SLA, health checks, failover
| Избыточность узлов, multi-AZ в подписях; health LB каждые 10 с; probes в K8s; DR-регион B. RTO/RPO детали — в 1.02/2.13/runbooks.
| ✅

| NFR-004
| Масштабирование, rolling updates, автоскейлинг
| HPA, rolling updates, node pools — в summary *k8s_prod* и *k8s_dr*.
| ✅

| NFR-005
| Безопасность: TLS, WAF, сегментация, IAM
| Зона *edge_security* (WAF/CDN), TLS до edge в summary; Keycloak (*vm_iam*), Vault (*vm_vault*). VPN/сегментация сети между ЦОД — в 3.01/инфраструктурных схемах, не как отдельный узел на этой диаграмме.
| ✅

| NFR-006
| Надёжность данных: репликация, бэкапы, RPO=0, гео-бэкапы ≥2 региона
| PostgreSQL master+replicas; MinIO в регионе A; два узла *backup-storage* в *region_backup_geography*. Расписание и политика retention — в 2.13/эксплуатации.
| ✅

| NFR-007
| Отказоустойчивость: избыточность, *географическая репликация ≥3 региона*, graceful degradation
| Три географических контура на диаграмме: регион A (основной), регион B (DR), регион C (гео-бэкапы) + edge как отдельная зона периметра; реплики/standby и warm K8s в B.
| ✅

| NFR-008
| Эффективность: CPU/memory, cache hit, пулы соединений
| Запас **~30%** по CPU/RAM в сайзинге; целевая утилизация **≤80%** зафиксирована в допущениях 4.01. Пороги в рантайме и пулы — 2.08/эксплуатация.
| ✅

| NFR-009
| Мониторинг, логи, трейсы, алертинг
| *ns_observability* и VM-хранилища (ES, метрики, Jaeger); агенты APM; связь с 2.09.
| ✅

|===

*Staging (`staging` в `deployment.c4`):* упрощённый контур (меньше узлов, один namespace приложений) — *достаточно* для NFR про тестирование миграций (NFR-006), но *не отображает* полную избыточность production; это ожидаемо для pre-prod.

*Итог:* deployment LikeC4 отражает *multi-region* (NFR-006, NFR-007), *периметр WAF/CDN* (NFR-005), *HA и probes* (NFR-003/004), *наблюдаемость* (NFR-009) и *ориентировочный сайзинг* (NFR-001, NFR-008 — см. <<sizing-deployment,сайзинг 4.01>>). **На других артефактах** остаются: числовые SLO latency (NFR-002), *фактический* cache hit и настройка пулов, детали VPN/сегментации (NFR-005), расписание бэкапов и RTO — *1.02*, *2.08*, *2.09*, *3.01*, *4.03*, runbooks.
